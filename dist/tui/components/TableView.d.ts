export declare function TableView({ rows, selectedIndex, visibleRowCount, emptyMessage, }: {
    rows: unknown[];
    selectedIndex: number;
    visibleRowCount: number;
    emptyMessage?: string;
}): import("react").JSX.Element;
